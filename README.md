# One-Sitting Idle: incremental games designed to end in 30–60 minutes, with a story and no ads

Live: https://one-sitting-idle.sociobot.in — built by the Param Factory (`static-web`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
